@model IEnumerable<RegistroNotas.Models.Periodo>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombrePeriodo)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombrePeriodo)
        </td>
        <td>
            @Html.ActionLink("Ver Cursos", "MostrarGrupos", new { idProfe =15, idPeriodo = item.IdPeriodo }) 
        </td>

    </tr>
    }

</table>



